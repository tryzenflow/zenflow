DATABASE_URL="postgres://admin:admin@localhost:5432/zenflow_sim?sslmode=disable&schema=public"
CACHE_URL="redis://localhost:6379"
CORS_ORIGIN="http://localhost:5173"
MAIL_TRANSPORT="smtp://localhost:1025"
MAIL_FROM="noreply@zenflow.io"
SESSION_SECRET='s3cr3t'
# Idle session lifetime in ms (rolling: active use keeps extending it). 7 days.
SESSION_TTL_MS=604800000
# Session cookie flags. Dev is same-origin over HTTP, so secure off + lax.
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
UPLOADED_FILES_DEST=uploads/
