DATABASE_URL="postgres://admin:admin@postgres:5432/zenflow-staging?sslmode=disable&schema=public"
CACHE_URL="redis://redis:6379"
CORS_ORIGIN="http://localhost:5173"
MAIL_TRANSPORT="smtp://mail:1025"
MAIL_FROM="noreply@zenflow.local"
SESSION_SECRET='s3cr3t'
# Idle session lifetime in ms (rolling: active use keeps extending it). 7 days.
SESSION_TTL_MS=604800000
# Session cookie flags. Staging is served over HTTP via Caddy :80, same-site on
# localhost, so secure off + lax.
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
UPLOADED_FILES_DEST=uploads/