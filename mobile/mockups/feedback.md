BLOCKING: Month view is broken
1. Tab bar (Day / Week / Month) should be flex with the display date (omit the circle dot svg)
2. Overlap tasks are too "red" (I'd prefer yellow with warning badge to differentiate with overdue tasks)
3. Rename "Reschedule" to "Move"
4. There's a task whose name isn't shown at all, only the time 8:00 - 8:30 -> confusing
5. Setting tab bar icons need to be consistent with calendar tab bar icons
6. Flex button where the second button is full width overflows the screen width (in Task Sheet)