export const DAILY_HORIZON = 1440;
export const TIME_GRANULARITY = 15;

/** First day of the week for all calendar views (1 = Monday), matching the mockups. */
export const WEEK_STARTS_ON = 1 as const;
