1. Select work days in settings should span the available space like in onboarding
2. Remove all the stripes in conflict tasks (just need)
3. Show task's name for 15min tasks in day and week views (don't just show the time)
4. It seems that tasks with very tight deadlines will be scheduled overdue if not possible to schedule before the deadline, can you double check that? If true, show a warning show overdue and it should have overdue style (red border)
5. The tag autocomplete is strange when the prefix is far from the remaining text
6. deadline should be selectable chips resembling @frontend\src\components\tasks\form\deadline-chip-field.tsx
7. add preference matrix and duration bias per tag like in @frontend\src\components\settings\preferences.tsx in settings screen
